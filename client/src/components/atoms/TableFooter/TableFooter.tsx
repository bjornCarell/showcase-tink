import React from 'react';
import MuiTableFooter, { TableFooterProps } from '@material-ui/core/TableFooter';

export const TableFooter: React.FC<TableFooterProps> = (props) => (
  <MuiTableFooter {...props} />
);
