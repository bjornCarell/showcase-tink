import React from 'react';
import MuiAutocomplete, { AutocompleteProps } from '@material-ui/lab/Autocomplete/Autocomplete';

// export const Autocomplete: React.FC<AutocompleteProps> = (props) => <MuiAutocomplete {...props} />;
